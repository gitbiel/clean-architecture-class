export interface UpdateProductInputDTO {
  id: string;
  name: string;
  description: string;
  price: number;
}

export interface UpdateProductOutputDTO {
  id: string;
  name: string;
  description: string;
  price: number;
}